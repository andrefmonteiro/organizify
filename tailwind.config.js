/** @type {import('tailwindcss').Config} */

// Where we define custom colors, theme extensions, breakpoints, font settings, plugin configurations

export default {
  content: [],
  theme: {
    extend: {},
  },
  plugins: [],
}

